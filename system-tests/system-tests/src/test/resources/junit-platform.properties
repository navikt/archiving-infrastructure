# Run the end-to-end tests in parallel against the single shared set of containers
# started in @BeforeAll. Parallelism is bounded; the failure test with seven archive
# attempts is the critical path. The poison-pill test and the 10x no-login loop take
# an exclusive "end-to-end" resource lock so they still run alone. See issue #86.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=4
junit.jupiter.execution.parallel.config.fixed.max-pool-size=4
