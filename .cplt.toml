# Generated by `cplt init` — review before trusting.
# Detected: Docker

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: (root)
allow_docker = true

[propose.allow]
ports = [2181, 5432, 8081, 8090, 8091, 8092, 9064, 9092]

